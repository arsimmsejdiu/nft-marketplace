import {
  Text,
  View,
  SafeAreaView,
  Image,
  StatusBar,
  FlatList,
} from "react-native";
import { COLORS, SIZES, SHADOWS, FONTS, assets } from "../constants";
import { CircleButton, RectButton, FocusedStatusBar, SubInfo } from "../components";

const Details = () => {
  return <Text>Details</Text>;
};

export default Details;
